# 部署用：复制为 .env，供 docker compose 与容器读取
# cp .env.example .env 后按实际环境修改

# ---------- 应用端口 ----------
PORT=3001

# ---------- 前端构建时（build-arg，影响打包进前端的 API/Socket 地址）----------
EXPO_PUBLIC_API_URL=https://your-domain.com
EXPO_PUBLIC_SOCKET_URL=https://your-domain.com

# ---------- Supabase（可选）----------
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_URL=
SUPABASE_ANON_KEY=

# ---------- TURN（服务端下发 ICE，app 容器运行时读取）----------
# 必须是客户端能访问的地址：同机部署时用公网 IP 或域名，与 EXTERNAL_IP 一致
# 例：TURN_URL=turn:your-domain.com:3478 或 turn:公网IP:3478
TURN_URL=
TURN_USERNAME=test
TURN_CREDENTIAL=test123
FORCE_TURN=

# ---------- Coturn 容器：external-ip（与 TURN_URL 的 host 一致）----------
# 部署到公网时必填，否则 TURN 分配地址错误
# 例：EXTERNAL_IP=your-domain.com 或 1.2.3.4
EXTERNAL_IP=
