/** Web 端 stub：codegenNativeComponent 仅用于原生，safe-area-context 在 web 用 View */
import React from 'react';
import { View } from 'react-native';
export default function codegenNativeComponent() {
  return View;
}
